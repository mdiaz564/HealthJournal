<!DOCTYPE html>
<html>
  <head>
    <title>VanillaRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://fonts.googleapis.com/css?family=Noto+Serif|Oxygen:300" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css">

  </head>

  <body>
  <% if current_user != nil %>
  <nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
      <a class="navbar-brand nav-title" href="<%= pages_path %>">My Health Journal</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    <div class="nav-link"><%= link_to "My Entries", pages_path %></div>
    <div class="nav-link"><%= link_to 'New Entry', new_page_path %></div>
    <div class="nav-link"><%= link_to "Edit profile", edit_user_registration_path %></div>
    <div class="nav-link"><%= link_to "Sign out", destroy_user_session_path, method: :delete %></div>
    </div>
  </nav>
  <% else %>

    <nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
      <a class="navbar-brand nav-title" href="<%= root_path %>">My Health Journal</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    <div class="nav-link"><%= link_to "Sign up", new_user_registration_path %></div>
    <div class="nav-link"><%= link_to "Sign in", new_user_session_path %></div>
    </div>
    </nav>
  <% end %>

    <%= alert %>
    <%#= notice %>
    <%= yield %>
  </body>
</html>
